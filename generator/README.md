HighPoint API Generator
=======================

`make app` — запуск сервера. По умолчанию API доступно по адресу:
[localhost:3011](http://localhost:3011/)

`node scripts/save.js` — создание документации в формате html в `/doc`.
